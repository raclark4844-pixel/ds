/** Hook copy for later city pages. Not a directory. */
export function MarketList() {
  return (
    <div className="mt-8 max-w-2xl text-sm text-muted">
      <p>
        Contractor copy stays ready for city and market pages later. We do not
        generate hundreds of near-duplicate city pages.
      </p>
    </div>
  );
}
